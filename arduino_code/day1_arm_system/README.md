# 로봇 팔 시스템 - 단계별 학습 가이드

## 📋 프로젝트 개요

4개의 서보 모터를 사용한 로봇 팔 제어 시스템을 단계별로 학습합니다.
간단하고 직관적인 코드로 핵심 동작을 이해할 수 있습니다.

## 🗂️ 폴더 구조

```
day1_arm_system/
├── 01_servo_motor_test/           # 서보 모터 동작 테스트
├── 02_joystick_servo_control/     # 조이스틱 + 서보 모터 제어
├── 03_serial_servo_control/       # Serial 원격 제어
├── 04_eeprom_automation/          # EEPROM 저장 자동화
├── 05_full_system/                # 부저 + LED Matrix (예정)
└── README.md                      # 이 파일
```

---

## 🔧 하드웨어 구성

### 필수 부품
- **Arduino Uno** (또는 호환 보드)
- **서보 모터 4개** (SG90 또는 MG90S)
  - 베이스 (좌우 회전)
  - 팔1 (상하)
  - 팔2 (상하)
  - 그립 (열기/닫기)
- **조이스틱 2개** (아날로그 조이스틱)
- **점퍼 와이어**
- **전원 (5V)**

### 핀 연결

| 부품 | 핀 번호 | 용도 |
|------|---------|------|
| **서보 모터** |
| 베이스 서보 | 4 | 좌우 회전 (50~130°) |
| 팔1 서보 | 5 | 상하 (70~120°) |
| 팔2 서보 | 6 | 상하 (60~120°) |
| 그립 서보 | 7 | 열기/닫기 (5~50°) |
| **조이스틱** |
| Joy1 X축 | A0 | 베이스 제어 |
| Joy1 Y축 | A1 | 팔1 제어 |
| Joy2 X축 | A2 | 팔2 제어 |
| Joy2 Y축 | A3 | 그립 제어 |

---

## 📚 단계별 학습

### 1️⃣ 서보 모터 동작 테스트

**파일:** `01_servo_motor_test/01_servo_motor_test.ino`

**목표:**
- 4개 서보 모터가 정상 작동하는지 확인
- 각 서보의 동작 범위 파악
- Serial로 현재 상태 확인

**동작:**
```
베이스: 50° → 130° → 90°
팔1: 70° → 120° → 90°
팔2: 60° → 120° → 90°
그립: 5° → 50° → 5°
```

**테스트 방법:**
1. Arduino IDE에서 열기
2. 보드 및 포트 선택
3. 업로드
4. Serial Monitor 열기 (9600 baud)
5. 서보 모터 동작 관찰

---

### 2️⃣ 조이스틱 입력 테스트

**파일:** `02_joystick_input_test/02_joystick_input_test.ino`

**목표:**
- 조이스틱 아날로그 입력 읽기
- 값의 범위 확인 (0~1024)
- 방향 판별 (↑↓)

**출력 예시:**
```
베이스:512 | 팔1:998 | 팔2:45 | 그립:500 → 팔1↑ 팔2↓
```

**테스트 방법:**
1. 업로드 후 Serial Monitor 열기
2. 조이스틱을 상하좌우로 움직이기
3. 값 변화 관찰
4. 임계값 확인 (↑ > 1000, ↓ < 100)

---

### 3️⃣ 조이스틱 + 서보 모터 제어

**파일:** `03_joystick_servo_control/03_joystick_servo_control.ino`

**목표:**
- 조이스틱으로 서보 모터 실시간 제어
- 각도 제한 (안전 범위 내에서만 동작)
- 부드러운 움직임

**제어 방법:**
- 조이스틱 위쪽: 각도 증가
- 조이스틱 아래쪽: 각도 감소
- 중립: 정지

**각도 범위:**
- 베이스: 50° ~ 130°
- 팔1: 70° ~ 120°
- 팔2: 60° ~ 120°
- 그립: 5° ~ 50°

**테스트 방법:**
1. 업로드
2. 조이스틱으로 로봇 팔 조작
3. Serial Monitor에서 각도 확인
4. 최대/최소 범위 확인

---

### 4️⃣ Serial 원격 제어

**파일:** `04_serial_servo_control/04_serial_servo_control.ino`

**목표:**
- Serial 명령으로 원격 제어
- 각 서보를 개별적으로 정밀 제어
- 블루투스 준비 (향후 확장)

**명령 형식:**
```
arm0_90_   → 베이스를 90도로
arm1_100_  → 팔1을 100도로
arm2_80_   → 팔2를 80도로
arm3_25_   → 그립을 25도로
```

**사용 예시:**
```
[수신] arm0_90
[실행] 베이스 90° → 90°
[완료]

[수신] arm1_110
[실행] 팔1 90° → 110°
[완료]
```

**테스트 방법:**
1. 업로드
2. Serial Monitor 열기
3. 명령 입력 (예: `arm0_90_`)
4. Enter 누르기
5. 서보 이동 확인

---

### 4️⃣ EEPROM 저장 자동화

**파일:** `04_eeprom_automation/04_eeprom_automation.ino`

**목표:**
- 조이스틱으로 위치 설정
- EEPROM에 최대 12단계 저장
- 저장된 동작 자동 재생
- 저장된 동작 무한 반복
- 전원이 꺼져도 저장 유지

**명령어:**
```
save  : 현재 위치 저장 (최대 12개)
play  : 저장된 동작 1회 재생
auto  : 저장된 동작 계속 반복 (stop으로 중지 가능)
stop  : 자동 반복 즉시 중지
clear : 모든 위치 삭제
list  : 저장된 위치 목록
```

**사용 흐름:**
```
1. 조이스틱으로 원하는 위치로 이동
2. 'save' 입력 → 위치 #1 저장
3. 조이스틱으로 다음 위치로 이동
4. 'save' 입력 → 위치 #2 저장
5. ... (최대 12개 위치 저장)
6. 'play' 입력 → 1회 자동 재생
   또는
   'auto' 입력 → 계속 반복 재생 (stop으로 즉시 중지)
7. 'stop' 입력 → 자동 반복 즉시 중지
```

**출력 예시:**
```
[명령] save
[저장 완료] 위치 #1 - 베이스:90 팔꿈치:90 손목:90 그립:5
저장된 위치: 1 / 12

[명령] save
[저장 완료] 위치 #2 - 베이스:45 팔꿈치:100 손목:80 그립:30
저장된 위치: 2 / 12

[명령] play
========================================
  자동 재생 시작 (1회)
========================================
저장된 위치 수: 2

→ 위치 #1 이동
  베이스:90 팔꿈치:90 손목:90 그립:5
→ 위치 #2 이동
  베이스:45 팔꿈치:100 손목:80 그립:30

========================================
  자동 재생 완료
========================================
```

### 자동 반복
```
[명령] auto
========================================
  자동 반복 모드 시작
========================================
저장된 위치 수: 2
'stop' 명령으로 중지
========================================

========================================
  반복 #1 실행 중...
========================================
→ 위치 #1 이동
  베이스:90 팔꿈치:90 손목:90 그립:5
→ 위치 #2 이동
  베이스:45 팔꿈치:100 손목:80 그립:30

========================================
  반복 #2 실행 중...
========================================
...

[명령] stop
========================================
  자동 반복 모드 중지
========================================
총 반복 횟수: 5
========================================
```

**EEPROM 구조:**
- 주소 0~3: 저장된 위치 개수
- 주소 4~7: 위치 #1 (4개 서보 각도)
- 주소 8~11: 위치 #2
- ...
- 최대 12개 위치 저장

**테스트 방법:**
1. 업로드
2. 조이스틱으로 첫 번째 위치 설정
3. Serial Monitor에서 `save` 입력
4. 두 번째, 세 번째 위치도 저장
5. `list` 명령으로 확인
6. `play` 명령으로 1회 재생
7. `auto` 명령으로 계속 반복 재생
8. `stop` 명령으로 중지
9. 아두이노 전원을 껐다 켜도 저장된 위치 유지

---

### 5️⃣ Bluetooth EEPROM 자동화

**파일:** `05_bluetooth_eeprom_automation/05_bluetooth_eeprom_automation.ino`

**목표:**
- Bluetooth로 무선 제어
- EEPROM에 최대 12단계 저장
- 저장된 동작 자동 재생
- 저장된 동작 무한 반복
- Serial과 Bluetooth 동시 지원 (디버깅)

**하드웨어 연결:**
```
Bluetooth 모듈 (HC-05/HC-06):
- VCC → 5V
- GND → GND
- TX  → Arduino 2번 핀
- RX  → Arduino 3번 핀
```

**명령어 형식 (중요!):**

① **Serial 통신** (PC Serial Monitor)
```
save  + Enter 키
play  + Enter 키
auto  + Enter 키
stop  + Enter 키
clear + Enter 키
list  + Enter 키
```

② **Bluetooth 통신** (앱 인벤터)
```
save_  ← 마지막 _ 필수!
play_  ← 마지막 _ 필수!
auto_  ← 마지막 _ 필수!
stop_  ← 마지막 _ 필수!
clear_ ← 마지막 _ 필수!
list_  ← 마지막 _ 필수!
```

**명령어 설명:**
```
save  : 현재 위치 저장 (최대 12개)
play  : 저장된 동작 1회 재생
auto  : 저장된 동작 계속 반복
stop  : 자동 반복 즉시 중지
clear : 모든 위치 삭제
list  : 저장된 위치 목록
```

**사용 흐름:**
```
1. Bluetooth 연결 (HC-05/HC-06) + Serial Monitor 열기 (디버깅용)
2. 조이스틱으로 원하는 위치로 이동
3. 'save_' 입력 (Bluetooth) 또는 'save' + Enter (Serial)
   → 위치 #1 저장
4. 조이스틱으로 다음 위치로 이동
5. 'save_' 입력 → 위치 #2 저장
6. ... (최대 12개 위치 저장)
7. 'play_' 입력 → 1회 자동 재생
   또는
   'auto_' 입력 → 계속 반복 재생
8. 'stop_' 입력 → 자동 반복 즉시 중지
```

**특징:**
- Serial과 Bluetooth 모두에서 명령 입력 가능
- 명령 출처 표시 (`[명령 from Serial]` / `[명령 from Bluetooth]`)
- 디버깅을 위한 Serial 모니터 동시 출력
- auto 실행 중 stop 명령으로 즉시 중지
- 전원이 꺼져도 저장 유지

**출력 예시 (Serial Monitor):**
```
[BT 수신] 's' (ASCII: 115)
[버퍼 추가] 현재: "s" (1자)
[BT 수신] 'a' (ASCII: 97)
[버퍼 추가] 현재: "sa" (2자)
[BT 수신] 'v' (ASCII: 118)
[버퍼 추가] 현재: "sav" (3자)
[BT 수신] 'e' (ASCII: 101)
[버퍼 추가] 현재: "save" (4자)
[BT 수신] '_' (ASCII: 95)
[명령 종료 문자: '_'] 명령어 버퍼: "save" (길이: 4)
========================================
[명령 처리 시작] from Bluetooth
받은 명령어: "save"
ASCII 코드: 115 97 118 101 
[인식] save 명령 실행
[저장 완료] 위치 #1 - 베이스:90 팔꿈치:80 손목:90 그립:15
저장된 위치: 1 / 12
========================================

[BT 수신] 'a' (ASCII: 97)
[버퍼 추가] 현재: "a" (1자)
[BT 수신] 'u' (ASCII: 117)
[버퍼 추가] 현재: "au" (2자)
[BT 수신] 't' (ASCII: 116)
[버퍼 추가] 현재: "aut" (3자)
[BT 수신] 'o' (ASCII: 111)
[버퍼 추가] 현재: "auto" (4자)
[BT 수신] '_' (ASCII: 95)
[명령 종료 문자: '_'] 명령어 버퍼: "auto" (길이: 4)
========================================
[명령 처리 시작] from Bluetooth
받은 명령어: "auto"
ASCII 코드: 97 117 116 111 
[인식] auto 명령 실행
========================================
  자동 반복 모드 시작
========================================
저장된 위치 수: 2
'stop' 명령으로 중지
========================================
```

**테스트 방법:**
1. 아두이노에 업로드
2. **반드시 Serial Monitor 열기** (9600 baud) - 디버깅 필수!
3. Bluetooth 모듈 연결 (스마트폰 앱)
4. Serial Monitor에서 `[BT 수신]` 메시지 확인
5. 조이스틱으로 위치 설정
6. 앱에서 `save_` 버튼 클릭 → Serial Monitor에서 수신 상태 확인
7. 여러 위치 저장 후 `play_` 또는 `auto_` 테스트
8. `stop_`으로 중지 테스트

**디버깅 팁:**
- Serial Monitor를 열어두면 모든 Bluetooth 수신 데이터가 표시됩니다
- `[BT 수신]` 메시지가 안 보이면 → Bluetooth 연결 실패
- `[명령 종료 문자: '_']` 메시지가 보이면 → 명령 전송 성공
- `[인식] xxx 명령 실행` 메시지가 보이면 → 명령 인식 성공

---

### 6️⃣ 전체 시스템 (예정)

**목표:**
- Bluetooth + EEPROM + 부저 + LED Matrix
- 상태 표시 (이모티콘)
- 사운드 효과

**추가 부품:**
- 피에조 부저
- 8x8 LED Matrix

---

## 🚀 사용 방법

### 학습 순서

1. **1단계** → 서보 모터 동작 확인
2. **2단계** → 조이스틱으로 실제 제어
3. **3단계** → Serial 원격 제어
4. **4단계** → EEPROM 자동화 (위치 저장/재생)
5. **5단계** → 고급 기능 (부저, LED Matrix) - 예정

### 문제 해결

#### 서보가 떨림
- 전원 부족: 별도 5V 전원 사용
- 부하 과다: 로봇 팔 무게 줄이기

#### 조이스틱 반응 없음
- 연결 확인: A0~A3 핀 확인
- Serial 값 확인: 2단계로 디버깅

#### Serial 명령 안됨 (3단계)
- 형식 확인: `arm0_90` + Enter
- 각도 범위 확인

#### Bluetooth 명령 안됨 (5단계)
- 형식 확인: `save_` (마지막 `_` 필수!)
- Serial Monitor에서 `[수신] save` 확인
- 연결 확인: Bluetooth 모듈 LED 깜빡임

#### save 명령 시 재부팅됨 (5단계)
**원인:** EEPROM 쓰기 중 서보 타이머 충돌 또는 전력 부족

**해결책:**
1. **외부 5V 전원 사용** (가장 중요!)
   - USB 전원만으로는 부족
   - 서보 4개 + Bluetooth 모듈 = 높은 전력 소비
   
2. **코드 수정 완료**
   - EEPROM 쓰기 전에 서보 detach()
   - 쓰기 후 다시 attach()
   - 타이머 충돌 방지

3. **테스트 방법**
   - Serial Monitor에서 `[OK #1]` 메시지 확인
   - 재부팅되지 않고 정상 저장되어야 함

---

## 📖 코드 구조

### 공통 구조 (1~4단계)

```cpp
#include <Servo.h>

Servo servo[4];           // 4개 서보 배열
int pin[4] = {4,5,6,7};   // 핀 번호
int angles[4] = {...};    // 현재 각도

void setup() {
  // 서보 초기화
  for (int i = 0; i < 4; i++) {
    servo[i].attach(pin[i]);
    servo[i].write(angles[i]);
  }
}

void loop() {
  // 각 단계별 로직
}
```

### 주요 개념

1. **배열 사용**: 4개 서보를 배열로 관리
2. **for 루프**: 반복 작업 간소화
3. **Serial 통신**: 디버깅 및 제어
4. **버퍼 관리**: 명령 파싱 (3단계)
5. **EEPROM**: 데이터 영구 저장 (4단계)

---

## 📝 추가 정보

### 각도 설정 팁

- **베이스**: 90°가 정면, 50°=왼쪽, 130°=오른쪽
- **팔1**: 90°가 수평, 작을수록 아래, 클수록 위
- **팔2**: 90°가 수평 (팔1과 동일)
- **그립**: 5°=열림, 50°=닫힘

### 조이스틱 보정

```cpp
// 임계값 조정
val > 1000  // 최대 (증가)
val < 100   // 최소 (감소)
```

### EEPROM 활용 팁

4단계에서 저장 용량 확장:
```cpp
#define MAX_POSITIONS 16  // 8개 → 16개로 확장
```

### 블루투스 확장

3~4단계 코드를 기반으로 블루투스 추가 가능:
```cpp
#include <SoftwareSerial.h>
SoftwareSerial BT(2, 3);  // RX, TX
```

---

## 🔗 참고 자료

- [Arduino Servo 라이브러리](https://www.arduino.cc/reference/en/libraries/servo/)
- [아날로그 입력 읽기](https://www.arduino.cc/reference/en/language/functions/analog-io/analogread/)
- [Serial 통신](https://www.arduino.cc/reference/en/language/functions/communication/serial/)

---

**최종 업데이트:** 2026년 1월
